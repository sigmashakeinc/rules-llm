{
  "description": "LLM and AI API governance rules for AI coding agents. Prevents API key exposure in source code, prompt injection via system prompts, runaway inference costs from uncapped max_tokens, PII leakage in LLM response logs, and unsafe temperature settings in production AI applications."
}
